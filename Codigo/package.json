{
  "name": "syssalud",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de gestión de turnos SysSalud - monorepo (Grupo 18)",
  "workspaces": [
    "syssalud-backend",
    "syssalud-frontend",
    "packages/shared-types"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=packages/shared-types",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "seed": "npm run seed --workspace=syssalud-backend",
    "dev:backend": "npm run start:dev --workspace=syssalud-backend",
    "dev:frontend": "npm run dev --workspace=syssalud-frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm run build --workspace=packages/shared-types && npm run build --workspace=syssalud-backend && npm run build --workspace=syssalud-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
